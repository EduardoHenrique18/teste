module.exports = class User {
  constructor (userName, password, dateOfBirth, email) {
    this.userName = userName
    this.password = password
    this.dateOfBirth = dateOfBirth
    this.email = email
  }
}
